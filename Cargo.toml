[package]
name = "feeds"
version = "0.0.0"
authors = ["Steven Sheldon"]

[dependencies]
chrono = "0.4"
feed-stream = { path = "feed-stream" }
fever-api = { path = "fever-api" }
iron = "0.5"
reqwest = "0.8"
serde = "1.0"
serde_json = "1.0"
url = "1.2"

[dependencies.diesel]
version = "1.0"
default-features = false
features = [
  "postgres",
  "chrono",
]

[workspace]
